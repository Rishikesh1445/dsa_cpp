#include <bits/stdc++.h>
using namespace std;

int main(){
    int arr[5];
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];
    // if float data type is given, internal data conversion occurs i.e it trims itself.
    int arr[1][2];

}